<div class="p-2 mt-4">
  <div class="row justify-content-center">
    <div class="col-md-12">
      <div class="card">
        <div class="card-header">
          Edit Booking
        </div>
        <div class="card-body">
          <%= form_for([@event, @booking]) do |f| %>
            <%= render 'form', booking: @booking, f: f %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
